---
id:          TASK-00276
title:       "Refresh docs/STYLE.md: Kotlin + TypeScript conventions, drift review since 2026-07-13"
status:      review
assignee:    CQR.08
created_by:  ARCH.00
created:     2026-09-10
priority:    normal
depends_on:  [TASK-00269]
blocks:      []
review_by:   [ARCH.00, CQR.08]
tags:        [docs]
---

Full-project review (`TASK-00269`) found `docs/STYLE.md`'s version history
stops at 2026-07-13 — it hasn't been touched since, despite cluster
replication, the vault/E2EE work, the web gateway, the Android client, and
corruption detection/repair all shipping afterward. It documents C/C++
conventions only; Kotlin (`android/app/src/main/java/...`) and TypeScript
(`web/src/*.ts`) now make up two full, shipped surfaces with no written
convention to check new code against.

## Acceptance criteria

- Read the actual existing Kotlin and TypeScript source to derive real,
  already-followed conventions (naming, error handling, null-safety idioms
  for Kotlin; module structure, typing strictness for TypeScript) —
  document what the code already does consistently, don't invent new
  rules unless a real inconsistency is found worth standardizing.
- Add dedicated Kotlin and TypeScript sections to `docs/STYLE.md`,
  matching its existing per-section structure (rule + one-line rationale).
- Spot-check whether any C/C++-side convention decisions made since
  2026-07-13 (e.g. patterns established during vault/gateway/Android/
  corruption-repair work) should be recorded here and aren't — report
  findings even if the answer is "still accurate as written."
- Bump `docs/STYLE.md`'s version history table.

## Notes

ARCH.00 [2026-09-10]: Filed from the full-project review's redundancy/
process findings (`TASK-00269`). `review_by: [ARCH.00]` rather than the
usual `CQR.08` since CQR.08 owns and is the assignee of this document —
ARCH.00 sanity-checks the result instead.

CQR.08 [2026-09-11]: **Correction, found during the review pass on
`TASK-00269`'s whole backlog**: `CLAUDE.md`'s routing rule 2 ("All other
tasks — must list at least CQR.08 in review_by") is unconditional; it has
no carve-out for the assignee also owning the document. `review_by`
changed to `[ARCH.00, CQR.08]` — both, not either — so this task
satisfies the rule literally while keeping ARCH.00's sanity-check for the
same reason it was added.

CQR.08 [2026-09-14]: Done. Read the actual Kotlin (all 27 files under
`android/app/src/main/java/com/vaporwault/client/`) and TypeScript (all
3 files under `web/src/`) source before writing anything — derived every
rule below from what the shipped code already does consistently, per
this task's own acceptance criteria, rather than generic
Kotlin/TypeScript best-practice divorced from this project.

**New `docs/STYLE.md` §17 (Kotlin, owned day-to-day by MOB.10)**: naming
(the `Field`-suffix backing-field convention, the `native*` JNI-crossing
prefix), null safety (`!!` used exactly once in the whole tree — a real,
checkable fact, not an aspiration; the nullable-fetch-vs-sentinel-action
split at the JNI boundary), error handling (no exceptions/no sealed
`Result` on the native-facing surface, a documented cross-thread
`lastError()` capture footgun this codebase has hit more than once),
threading (no coroutines anywhere — confirmed by grep, not assumed;
plain threads + `runOnUiThread` or `TransferManager`'s
`JobScheduler`/UIDT path), file/module structure, comments (KDoc, no
tags — the same rule as the C side's §10, kept deliberately consistent),
Android-specific idioms (`RecyclerView.Adapter` shape, SAF staging), and
a dedicated "native-bridge idioms" subsection (the
`AutoCloseable`-wrapping-one-handle shape duplicated between `VwClient`/
`VwVault` on purpose rather than factored out, `ByteArray`-never-`String`
for secrets crossing the boundary with independent zeroing on both
sides).

**New §18 (TypeScript, owned day-to-day by WEB.09)**: the
must-use-`.js`-extension-in-imports gotcha (a real, compile-time-silent,
runtime-breaking mistake specific to this project's no-bundler setup —
worth stating explicitly), the three-files-one-job module structure,
the `ApiResult<T>` discriminated-union error-handling shape (never
`{ data: T | null }`), the `void handleX()` unawaited-promise
convention, and the `el<T>()` throw-on-missing-DOM-element pattern.

**§6 (Memory) gained two findings from the C/C++ drift spot-check**,
both real, both already load-bearing in code shipped since this
session's own earlier tasks: the "borrowed pointer" convention (grepped
30 existing uses across `src/server/`, not just my own recent additions
— `vw_cluster_open`'s five pre-existing borrowed parameters plus the
sixth `TASK-00285` just added) and the in-memory-auxiliary-array-in-
lockstep-with-a-slot-index idiom (`vw_vault.c`'s `pin_counts`,
`TASK-00290`, explicitly modeled again for `vw_cluster.c`'s
`client_conn_counts`, `TASK-00285` — two real instances of a deliberately
reused pattern, not a rule invented from one data point).

**Spot-checked sections 1–16 against code shipped since 2026-07-13**
(cluster replication, vault/E2EE, web gateway, Android, corruption
detection/repair — this whole session's own work included): naming
(§2), error handling (§5), concurrency (§8), and security-sensitive
code (§9) all still hold exactly as written — every lock added this
session documents which resource it protects and is never held across
I/O; every new function returns `vw_err_t` through the same out-pointer
convention; no new secret ever reached a log call. No changes needed to
those sections beyond the two §6 additions above. Reporting this
explicitly per the acceptance criteria's own instruction to report
"still accurate as written" findings, not just changes.

**Verified every claim before writing it down**, not just trusted the
research pass: independently grepped the `!!` count (1, exact match),
confirmed no coroutine usage anywhere (`suspend`/`Flow`/
`kotlinx.coroutines` all absent), and read `VwClient.kt`/`VwNative.kt`
directly to confirm the `handleField` backing-field pattern and the
nullable-fetch-vs-sentinel-action split at the JNI boundary before
citing them as rules.

Moving to `review` for ARCH.00 + CQR.08 per this task's own corrected
`review_by`.
