---
id:          TASK-00277
title:       "Formalize phase-row-at-close-time discipline for ARCHITECTURE.md; note GUI/web parity cadence"
status:      review
assignee:    ARCH.00
created_by:  ARCH.00
created:     2026-09-10
priority:    normal
depends_on:  [TASK-00269]
blocks:      []
review_by:   [CQR.08]
tags:        [process]
---

Full-project review (`TASK-00269`) found `ARCHITECTURE.md`'s
Implementation Phases table has drifted stale from actual `TODO/` state
three separate times (`2026-07-29` initial review, `TASK-00118`,
`TASK-00204`) — each time caught only by a manual backfill audit weeks
later, sometimes finding entire shipped phases missing a row entirely.
`ARCHITECTURE.md`'s own closing note (2026-08-26) already proposes the
real fix: add the phase row when the design task closes, not on a
periodic backfill pass. That proposal has never been formalized anywhere
agents would actually see it before closing a design task.

Separately, the same review's domain-density read found GUI.03 (22 tasks)
and WEB.09 (22 tasks) both mid-pack relative to SRV.01 (65) despite being
full UI surfaces, and that GUI audit-log/search/version-history views have
repeatedly landed *after* the corresponding protocol/server work rather
than in step. Worth naming as a scheduling pattern for future protocol-
touching phases, not a quality problem.

## Acceptance criteria

- `CLAUDE.md`'s "Step 8 — Milestone closure (ARCH.00)" (or a more
  appropriate step — implementer's call after re-reading the workflow
  section) gains an explicit instruction: update `ARCHITECTURE.md`'s
  Implementation Phases table in the same change that marks a design task
  `done`, not as a separate later pass.
- A short note added near that same workflow step (or in the Coordination
  Workflow's Step 5 GUI integration section) that a protocol-touching
  design should default to filing GUI.03/WEB.09 integration tasks
  alongside the SRV.01/CLI.02 ones, not as a reactive follow-up once a gap
  is later noticed — matching how `TASK-00185`'s breakdown already did
  this correctly (protocol → server → client → GUI → web, filed together).
- No change to the actual Status Lifecycle or TODO-folder rules — this is
  additive process guidance, not a rewrite of the existing protocol.

## Notes

ARCH.00 [2026-09-10]: Filed from the full-project review's process
findings (`TASK-00269`).

ARCH.00 [2026-09-11]: Implemented both edits directly in `CLAUDE.md`.

- Step 8 (Milestone closure) now explicitly requires updating
  `ARCHITECTURE.md`'s Implementation Phases table in the same change
  that closes the milestone, with the drift history cited inline so a
  future reader understands *why* this is a hard requirement rather than
  routine housekeeping easy to defer.
- Step 1 (Design & decompose) now instructs ARCH.00 to file GUI.03/WEB.09
  integration tasks in the same breakdown as SRV.01/CLI.02 for any
  wire-touching feature, citing `TASK-00185`'s breakdown as the pattern
  to follow and this review's own domain-density finding as the evidence
  the reactive pattern was a real, recurring gap. Placed at Step 1 rather
  than Step 5 (GUI integration) since Step 5 already assumes the task
  exists and just describes when GUI.03 picks it up — the actual gap was
  upstream, at task-filing time.
- No change to the Status Lifecycle or TODO-folder rules, per this task's
  own acceptance criteria — additive guidance only.

This session's own `VAULT_DELETE` work (`TASK-00274`–`00280`) already
followed the corrected Step 1 pattern in practice (protocol → server →
client/CLI → GUI → web gateway, all filed together in `TASK-00269`'s
breakdown) — this task just makes that the documented default going
forward instead of something that happened to be done right once.
