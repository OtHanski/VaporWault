---
id:          TASK-00272
title:       "Resolve stale logging-subsystem TODO in vw_server_core.c"
status:      review
assignee:    SRV.01
created_by:  ARCH.00
created:     2026-09-10
priority:    low
depends_on:  [TASK-00269]
blocks:      []
review_by:   [CQR.08]
tags:        [cleanup]
---

Full-project review (`TASK-00269`) found a `TODO: log at WARN when the
logging subsystem is available` (plus a commented-out
`vw_log_warn(...)` call) in `vw_server_core.c`'s session-resume path,
next to a best-effort session-revoke call. Verified: no project-wide
logging subsystem exists anywhere in the tree. The closest thing is a
`LOG_DEBUG` macro defined locally inside `vw_file_handlers.c` (line 32)
and used only in that one file — nothing `vw_server_core.c` can reach.
The TODO has been waiting on infrastructure that was never built and
isn't otherwise planned.

## Acceptance criteria

- Comment rewritten to accurately describe current (permanent, not
  temporary) behavior: the revoke failure is silently ignored, best-effort,
  matching the surrounding comment's own already-stated reasoning (old
  token remains valid until natural expiry; acceptable since the new token
  is already in hand).
- No dead/commented-out code left behind.
- No behavior change — this is a comment-and-dead-code cleanup only.

## Notes

SRV.01 [2026-09-10]: Rewrote the comment and removed the empty
`if (revoke_err != VW_OK ...) { /* TODO ... */ }` block (the condition had
no side effect either way). Verified via WSL `build-wsl-werror`:
`vw_server_lib` rebuilds clean under `-Werror`, full `ctest` suite (25/25)
still passes. Filed straight to `review`.

If a real logging subsystem is ever wanted project-wide, that's separate,
larger design work (not filed here — no signal this is currently wanted
beyond this one stale comment).
