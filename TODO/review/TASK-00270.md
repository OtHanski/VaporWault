---
id:          TASK-00270
title:       "Remove dead vw_invite_get/vw_invite_mark_used (superseded by vw_invite_claim)"
status:      review
assignee:    SRV.01
created_by:  ARCH.00
created:     2026-09-10
priority:    low
depends_on:  [TASK-00269]
blocks:      []
review_by:   [CQR.08]
tags:        [cleanup]
---

Full-project review (`TASK-00269`) found `vw_invite_get()`
(`src/server/vw_invite.c`/`.h`) has zero call sites anywhere in `src/` —
verified by grep, not inferred from the task title alone. It was superseded
by the atomic `vw_invite_claim()` back in `TASK-00045` (fixed a TOCTOU race
between validating and consuming an invite); CQR.08 flagged
`vw_invite_get` for removal at the time and it was never actually removed.
While removing it, found its sibling `vw_invite_mark_used()` is *also*
dead (same TASK-00045 history — replaced by `vw_invite_claim`'s atomic
write, zero remaining call sites) and not flagged before; removed both in
one pass rather than leaving one dead half behind.

## Acceptance criteria

- `vw_invite_get`/`vw_invite_mark_used` removed from `vw_invite.c` and
  `vw_invite.h` entirely (declaration, definition, doc comments).
- `vw_invite.c`'s file-header comment and `vw_invite_claim`'s doc comment
  updated to no longer reference the removed functions as if they still
  existed.
- Full build (`vw_server_lib` and everything that links it) still compiles
  clean under `-Werror`; no test referenced either function (confirmed, no
  `tests/unit/test_vw_invite.c` exists).

## Notes

SRV.01 [2026-09-10]: Removed both functions and updated
`vw_invite.h`'s `vw_invite_claim` doc comment to explain the history
in place of the old "use this instead of X+Y" phrasing (X and Y no
longer exist to reference). Verified via WSL `build-wsl-werror`:
`vw_server_lib`, `vapourwaultd`, `vapourwault-server-cli` all rebuild
clean with `-Werror`, and the full `ctest` suite (25/25) still passes.
Filed straight to `review` since the change is complete and verified;
awaiting CQR.08 sign-off.
