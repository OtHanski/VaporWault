---
id:          TASK-00294
title:       "Server: populate server_version in HELLO_OK/VERSION_REJECT"
status:      review
assignee:    SRV.01
created_by:  ARCH.00
created:     2026-09-15
priority:    normal
depends_on:  [TASK-00292]
blocks:      [TASK-00303]
review_by:   [CQR.08]
tags:        [server]
---

`TASK-00292` adds the wire capability to carry a `server_version` hint in
`HELLO_OK`/`VERSION_REJECT`, and the `vw_proto_negotiate` API to encode it,
but the server never actually populates it yet.

At `vw_server_conn_handle` (`vw_server_core.c:949`, negotiate call at `:963`),
pass the server's own `VW_VERSION_STRING` (already compiled into every
binary via `vw_version.h`) as the `server_version` to encode into whichever
of `HELLO_OK`/`VERSION_REJECT` actually gets sent. This must work on **both**
paths — the ordinary successful-negotiation path and the version-rejected
path — since the rejected path is the more important one for a client that's
too old to even connect (see `TASK-00291`'s design notes on why).

## Acceptance criteria

- A client connecting to this server always sees `out_hint->present == 1`
  with the server's real version string, on both `HELLO_OK` and
  `VERSION_REJECT`.
- No wire/behavior change for a server-side negotiate call that's actually
  server-to-server (cluster links, `vw-cluster/1` ALPN) — confirm those call
  sites are unaffected or explicitly pass `NULL`/don't populate.
- Existing negotiation tests still pass; add one asserting the populated
  `server_version` matches `VW_VERSION_STRING`.

## Notes

SRV.01 [2026-09-15]: Implemented as designed, with one deviation from the
task brief's original sketch: rather than having `vw_server_core.c` include
the generated `vw_version.h` directly (which would have required adding the
`vw_version` INTERFACE library to `vw_server_lib` and to all 5 test targets
that compile `vw_server_core.c` as a raw source — `test_vw_file_handlers`,
`test_auth_handshake`, `test_file_list_vault_id`, `test_cluster_repair_fetch`,
`test_repair_pipeline` — a lot of build-file churn for one string), followed
the module's own existing `vw_server_ctx_set_*` borrowed-pointer convention
(same shape as `vw_server_ctx_set_notify`/`_set_oplog`/etc.): new
`vw_server_ctx_set_version(vw_server_ctx_t *ctx, const char *version)`,
new `server_version` field on `struct vw_server_ctx` (defaults to `NULL` via
the existing `calloc`, so every caller that never calls the new setter —
i.e. every existing test — is unaffected). `vw_server_main.c` calls it once
at startup with `VW_VERSION_STRING`, right after `vw_server_ctx_open`.
Wired into the one real call site, `vw_server_conn_handle`
(`vw_server_core.c:963`): `ctx->server_version` replaces the `NULL` literal
`TASK-00292` left there.

Confirmed the cluster/`NODE_HELLO` path (`vw_cluster.c`) is unaffected — it
has no `vw_proto_negotiate` call site at all (verified by grep, matching
`TASK-00292`'s own research).

New TC-4 in `tests/integration/test_auth_handshake.c` (reuses that file's
existing real server-thread infrastructure rather than building a new one):
sets `vw_server_ctx_set_version(srv_ctx, "9.9.9-real-server-test")`, then a
real client connects and negotiates only (no `AUTH_REQUEST`), asserting the
real wire response's hint matches — proves the full path from config through
`vw_server_conn_handle`'s real call site to the wire, not just the setter's
own plumbing in isolation.

Full build (MSVC, all targets) clean. Full `ctest` (minus the two
pre-existing, unrelated cluster-test failures): 24/24 pass.

Moving to `review` for CQR.08 per `review_by`.

