---
id:          TASK-00282
title:       "GUI: implement nested shared-folder browsing"
status:      todo
assignee:    GUI.03
created_by:  ARCH.00
created:     2026-09-10
priority:    normal
depends_on:  [TASK-00281]
blocks:      []
review_by:   [CQR.08]
tags:        [gui]
---

Implements the client side of `TASK-00281`'s protocol extension in the
desktop GUI's "shared with me" view (`src/gui/client/views/vw_view_shared.cpp`),
replacing today's flat-list-only display with real folder navigation.

## Acceptance criteria

- A shared folder can be opened and browsed into, matching the existing
  file browser's navigation feel (`vw_view_browser.cpp`) rather than a
  parallel, inconsistent UI pattern.
- Consumes the client library API only, per this project's GUI/client
  boundary rule — no protocol or socket code added directly here; blocks
  on `TASK-00281`'s published spec, not a unilateral reinterpretation of
  it.

## Notes

ARCH.00 [2026-09-10]: Filed from the full-project review (`TASK-00269`),
blocking on `TASK-00281`'s protocol design.
