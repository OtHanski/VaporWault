---
id:          TASK-00293
title:       "Crypto: ECDSA P-256 signature verification + update-manifest signing key"
status:      done
assignee:    PRT.04
created_by:  ARCH.00
created:     2026-09-15
priority:    high
depends_on:  [TASK-00291]
blocks:      [TASK-00297, TASK-00301]
review_by:   [SEC.07, CQR.08]
tags:        [security-sensitive, crypto]
---

New signature-verification primitive plus a new dedicated signing keypair for
the update-manifest scheme (`TASK-00291`). See `ARCHITECTURE.md`'s "Client
auto-update: manifest signing key" row for the full rationale (why a new key
distinct from the GPG packaging key, why ECDSA over GPG).

## New `vw_crypto` primitive

```c
#define VW_ECDSA_P256_PUBKEY_BYTES 65 /* uncompressed SEC1 point: 0x04||X||Y */

vw_err_t vw_crypto_ecdsa_p256_verify(const uint8_t pubkey[VW_ECDSA_P256_PUBKEY_BYTES],
                                      const uint8_t hash[VW_HASH_BYTES] /* SHA-256 digest */,
                                      const uint8_t *sig_der, size_t sig_der_len);
```

Implement via `mbedtls_pk_setup`/`mbedtls_ecp_point_read_binary`/
`mbedtls_pk_verify` (same `mbedtls_pk_ec()` accessor pattern `vw_acme.c:552`
already uses, same justifying comment carried over). Returns `VW_OK` on
match, `VW_ERR_CRYPTO_SIG_INVALID` (new error code, next free slot `611` in
the 600-block, `vw_proto.h`) otherwise — follows the `vw_crypto_argon2id_verify`
return-code convention, not a separate bool out-param.

Also add the update-engine's own local error codes (client/daemon-local,
next free slot in the 700-block after `VW_ERR_IPC_NOT_RUNNING = 700`):
`VW_ERR_UPDATE_MANIFEST_INVALID`, `VW_ERR_UPDATE_MANIFEST_ROLLBACK`,
`VW_ERR_UPDATE_NET`, `VW_ERR_UPDATE_ASSET_MISMATCH`.

## Keypair generation and custody

- New committed script `packaging/signing/gen_update_keypair.sh` (offline
  only, never run in CI): `openssl ecparam -name prime256v1 -genkey` +
  `openssl ec -pubout`.
- Private key (PEM) goes into a new GitHub Actions secret
  `UPDATE_SIGNING_KEY` — same custody pattern as the existing
  `GPG_SIGNING_PRIVATE_KEY`.
- Public key committed twice, kept in sync by a test:
  1. `packaging/signing/update-manifest-pubkey.pem` (human/auditor-readable).
  2. `src/core/vw_update_pubkey.h` — generated once, hand-committed
     `static const uint8_t VW_UPDATE_MANIFEST_PUBKEY[VW_ECDSA_P256_PUBKEY_BYTES] = {...};`
     (raw SEC1 point bytes), compiled directly into every client binary.
- New unit test parses the `.pem` at test time and asserts its decoded raw
  point bytes equal `VW_UPDATE_MANIFEST_PUBKEY`, so the two committed forms
  can never silently drift apart.

## Acceptance criteria

- `vw_crypto_ecdsa_p256_verify` unit-tested: valid signature accepts, flipped
  bit in signature/hash/pubkey rejects, malformed DER rejects, does not crash
  on truncated/garbage input.
- Keypair generated, private key added to GitHub Actions secrets (manual step
  — document exactly what the operator must do, since this agent cannot add
  repo secrets itself).
- Both committed public-key representations present and the drift-check test
  passes.
- SEC.07 sign-off given `security-sensitive` tag — this key's compromise
  would let an attacker sign a malicious update manifest.

## Notes

PRT.04 [2026-09-15]: Implemented as designed, with two adjustments found
during implementation:

- **Error code slot**: used `VW_ERR_CRYPTO_SIG_INVALID = 501` (the "Crypto"
  category, `500`, had only one entry with `501-599` free) rather than the
  design brief's suggested `611` — `TASK-00292`'s own notes flagged this
  during its implementation; `611` would have landed in the "File transfer"
  600-block, a worse semantic fit. The four `VW_ERR_UPDATE_*` codes use
  `802-805` (after the existing client-local `700`/`800`/`801` codes).
- **Public-key verification approach**: rather than reaching into
  `mbedtls_ecp_keypair`'s `MBEDTLS_PRIVATE()` `grp`/`Q` fields directly (the
  design brief's sketch), `vw_crypto_ecdsa_p256_verify` builds a small fixed
  26-byte DER SubjectPublicKeyInfo prefix around the raw point and calls
  `mbedtls_pk_parse_public_key()` — the public, documented API. Verified
  correct against real OpenSSL-generated key/signature material (not just
  self-consistency), see the unit test below.
- `vw_crypto_ecdsa_p256_verify()` implemented in `src/core/vw_crypto.{h,c}`,
  `#include <mbedtls/pk.h>` added.
- Keypair generated via the new committed `packaging/signing/gen_update_keypair.sh`.
  Public key committed at `packaging/signing/update-manifest-pubkey.pem`;
  compiled-in raw-bytes form at new `src/core/vw_update_pubkey.h`
  (`VW_UPDATE_MANIFEST_PUBKEY`).
  **The private key (`update_signing_key.pem`) is NOT committed anywhere —
  it currently exists only in this session's scratchpad directory
  (session-isolated temp, never inside the git working tree). A human
  operator with GitHub repo admin access must: (1) copy its contents into a
  new `UPDATE_SIGNING_KEY` GitHub Actions secret, and (2) delete the
  scratchpad copy once that's done. This agent has no ability to create
  repo secrets itself — flagging this as the one manual step blocking
  `TASK-00301`'s CI signing step from actually working end-to-end, though
  the CI script/logic itself can be written and tested independently of
  whether the secret exists yet.**
- New unit tests in `tests/unit/test_vw_crypto.c` (16 new assertions):
  7 `vw_crypto_ecdsa_p256_verify` cases using a real OpenSSL-signed
  known-answer vector (proves interop with the actual production signer,
  not just self-consistency) — valid vector accepts; flipped signature
  byte, flipped hash byte, wrong pubkey, non-`0x04`-prefixed pubkey,
  truncated DER signature, and each NULL/zero-length argument all correctly
  rejected with no crash. Plus a drift-check case
  (`vw_update_pubkey: compiled-in constant matches committed .pem`) that
  independently parses the committed `.pem` at test time (via
  `mbedtls_pk_parse_public_key` on file bytes read directly — discovered
  `mbedtls_pk_parse_public_keyfile` is unavailable, gated behind
  `MBEDTLS_FS_IO` which this project's minimal mbedTLS config doesn't
  enable) and compares its decoded raw point against
  `VW_UPDATE_MANIFEST_PUBKEY`, so the two committed representations can
  never silently drift apart. New `VW_REPO_SOURCE_DIR` compile definition
  added to `tests/unit/CMakeLists.txt` for this one test target so it can
  locate the `.pem` at an absolute path regardless of the build's working
  directory.
- Full build (MSVC, all targets) clean. Full `ctest` (minus the two
  pre-existing, unrelated cluster-test failures already confirmed in
  `TASK-00292`'s notes): 24/24 pass, including all 99 `test_vw_crypto`
  assertions.

Moving to `review` for SEC.07 + CQR.08 per `security-sensitive`.

**SEC.07 + CQR.08, 2026-09-16**: Reviewed as part of a full-milestone pass
across `TASK-00292`–`TASK-00298`. Zero findings. `vw_crypto_ecdsa_p256_verify`
builds a correct fixed SPKI DER prefix, uses only the public
`mbedtls_pk_parse_public_key`/`mbedtls_pk_verify` API (no unsafe reach into
private mbedTLS internals), and fails closed on any parse/verify error.
`VW_UPDATE_MANIFEST_PUBKEY` is a plain `static const` array with no code
path that could load a different key from disk/env. `status: done`.

