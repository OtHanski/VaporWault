---
id:          TASK-00292
title:       "Protocol: extend HELLO_OK/VERSION_REJECT with server-version hint"
status:      done
assignee:    PRT.04
created_by:  ARCH.00
created:     2026-09-15
priority:    high
depends_on:  [TASK-00291]
blocks:      [TASK-00294, TASK-00295]
review_by:   [CQR.08]
tags:        [protocol]
---

Extend the wire handshake so the client can learn the server's software
version, in both the normal-handshake case and the hard-rejection case. See
`TASK-00291`/`ARCHITECTURE.md`'s "Client auto-update: version exchange" row
for the full rationale.

## Wire layout

`HELLO_OK` (`vw_proto.h:395-398`, currently 18 bytes: `negotiated_version` u16
+ `server_id[16]`): append, only when `total_len > 18`:
- `update_ext_ver` (u8) — `1` for this design; a reader seeing a higher value
  it doesn't understand treats the block as absent.
- `server_version_len` (u8), `0..31`.
- `server_version` (ASCII, not NUL-terminated), `server_version_len` bytes.

Max extended size 51 bytes — inside the client's existing 64-byte read buffer.

`VERSION_REJECT` (`vw_proto.h:400-403`, currently 4 bytes: `min_version`/
`max_version` u16 each): same trailing block appended at offset 4. Max
extended size 37 bytes.

`HELLO` (client→server) is deliberately **unchanged** — only the server's
version is exchanged in v1.

No `VW_PROTO_VERSION_CURRENT` bump — this is a purely additive extension,
same precedent as `TASK-00106`/`TASK-00186`.

## API change

`vw_proto_negotiate()` (`vw_proto.c:165`, declared `vw_proto.h:668`) gains a
new optional out-parameter:

```c
typedef struct {
    int  present;
    char server_version[32]; /* NUL-terminated; "" if !present */
} vw_proto_update_hint_t;

vw_err_t vw_proto_negotiate(vw_conn_t *conn, int is_server,
                             uint16_t *out_version,
                             vw_proto_update_hint_t *out_hint /* may be NULL */);
```

Critical detail: the client branch must populate `*out_hint` **before**
returning, on *both* the `VW_OK` (`HELLO_OK`) path and the
`VW_ERR_PROTO_VERSION` (`VERSION_REJECT`) path — `vw_client_sess_t` is
destroyed on the reject path before the caller ever sees it, so the hint
cannot live inside the session object. Bounds-check `server_version_len <= 31`
and against the buffer's actual received length before copying.

Server branch never populates `out_hint` (nothing to report about itself in
this direction); server callers pass `NULL`.

Update the 5 call sites: `vw_client_core.c:207,238,258,2223` (client, each
needs a new trailing `vw_proto_update_hint_t *out_hint` parameter threaded
through their own public signatures, may be `NULL`) and
`vw_server_core.c:963` (server, passes `NULL`) — actually wiring the server's
own `server_version` into the *sent* `HELLO_OK`/`VERSION_REJECT` is
`TASK-00294`'s job, not this task's; this task only adds the wire
encode/decode capability and the negotiate API.

`docs/PROTOCOL.md`: new dated revision entry documenting the extension, the
`update_ext_ver` versioning-of-the-extension convention, the 64-byte
compatibility ceiling, and that `HELLO` is intentionally unchanged.

## Acceptance criteria

- Old client ↔ new server: old client's fixed-size read + `plen < N` check
  already tolerates the extension untouched — verify with a real
  old-binary-vs-new-server test, not just code inspection.
- New client ↔ old server: `out_hint->present == 0`, no behavior change.
- `docs/PROTOCOL.md` revision entry published.
- Unit tests for the new encode/decode + bounds checks (malformed
  `server_version_len`, oversized value, truncated payload).

## Notes

PRT.04 [2026-09-15]: Implemented as designed.

- `vw_proto_update_hint_t` added (`vw_proto.h`), `vw_proto_negotiate()` gains
  `server_version` (server-side input) and `out_hint` (client-side output)
  parameters. All 6 call sites updated (4 in `vw_client_core.c`, 1 in
  `vw_server_core.c`, 1 in `src/gui/server/vw_server_conn.cpp` — the server
  GUI acting as a client); all pass `NULL, NULL` for now (real wiring is
  `TASK-00294`'s job, as scoped).
- Error-code numbering note: the design brief suggested slot `611` for a
  future `VW_ERR_CRYPTO_SIG_INVALID` (that's `TASK-00293`'s code, not
  touched here) — on inspection, `vw_proto.h`'s `500` "Crypto" category has
  only one entry (`VW_ERR_CRYPTO = 500`) with `501-599` free, which is a
  more natural home than the `600`-block ("File transfer"-specific errors).
  Flagging for `TASK-00293` to use `501` instead of `611`.
- Full build (MSVC, all targets) clean. Full `ctest` run: confirmed the two
  pre-existing cluster-test failures (`integration_cluster_repair_fetch`,
  `integration_repair_pipeline`) are **not** a regression — reproduced
  identically via `git stash` against the unmodified tree (same port-bind
  failure, same assertion lines), unrelated to this change (neither test
  touches `vw_proto_negotiate`). All 23 other tests pass, including the new
  ones below.
- New `tests/integration/test_proto_update_hint.c` (5 cases, real live TLS
  connections, no mocking): real HELLO_OK carries the hint; real HELLO_OK
  with nothing configured to advertise carries no hint (default/legacy
  shape unaffected); a hand-crafted old-shaped HELLO against a real server
  proves the VERSION_REJECT-with-hint wire bytes are well-formed; a
  hand-crafted VERSION_REJECT proves the real client-side decode populates
  `out_hint` on the reject path specifically (the load-bearing design
  point — session object doesn't survive that path); a truncated/malformed
  extension is treated as absent, never fails an otherwise-valid handshake.
  All 5 pass.
- `docs/PROTOCOL.md`: revision 35 published (§6.2/§6.3 tables extended,
  new §6.4 explaining the extension, header revision count bumped 34→35).

Moving to `review` for CQR.08 per `review_by`.

**CQR.08 + SEC.07, 2026-09-16**: Reviewed as part of a full-milestone pass
across `TASK-00292`–`TASK-00298`. Zero findings. `append_update_hint`/
`parse_update_hint` are correctly bounds-checked, every `_ex` negotiate
call site populates `out_hint` on both the success and hard-reject paths
as designed, and `docs/PROTOCOL.md` §6.4 matches the implementation
exactly. `test_proto_update_hint.c` poisons the struct before calling to
prove full initialization and covers both paths plus a truncated-extension
case. `status: done`.

