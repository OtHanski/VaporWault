---
id:          TASK-00184
title:       "Integration tests for desktop version history/restore"
status:      done
assignee:    QA.06
created_by:  ARCH.00
created:     2026-08-25
priority:    normal
depends_on:  [TASK-00182, TASK-00183]
blocks:      []
review_by:   [CQR.08]
tags:        [test]
---

Regression coverage for `TASK-00182`/`TASK-00183`, following standing QA.06
policy of a test per newly-exposed capability.

## Work

- Python integration test driving the daemon IPC directly: version list
  returns the expected entries after several edits; restore changes HEAD;
  restore is rejected while on fallback (`TASK-00173`'s test fixtures should
  already give a way to force fallback mode — reuse, don't reinvent);
  restore is rejected for a view-only grantee.
- CLI-level test: `vapourwault-cli version list`/`version restore` against
  a real running daemon+server pair.

## Acceptance criteria

- All of the above pass against freshly-built binaries (not stale ones —
  see `TASK-00173`'s notes on the `build-gw-e2e`/`VW_BUILD_TESTS=OFF` trap
  before trusting any `ctest` run here).

## Notes

<!-- Agents append notes below with their ID and date. Do not delete prior notes. -->

ARCH.00 [2026-08-25]: Filed alongside `TASK-00182`/`TASK-00183`.

QA.06 [2026-08-26]: All three listed acceptance items covered, each
verified against freshly-built binaries (`build-gw-e2e` on WSL/GCC,
rebuilt this session after clearing its stale `VW_WERROR=ON` per
`TASK-00215`'s note — not stale binaries):

- `tests/integration/test_cli_version_history.py`: daemon IPC + CLI
  version list/restore happy path (via the CLI, which is the only way
  a Python test can reach the new IPC without hand-rolling a second
  wire decoder — matches this suite's existing convention of driving
  real binaries rather than reimplementing their protocols); and the
  view-only-grantee-cannot-restore-a-shared-path case (found to be a
  `NOT_FOUND`, not a permission distinction, for the reason recorded on
  `TASK-00214` — the test's docstring and assertions were corrected to
  claim exactly that, not more).
- `tests/integration/test_cli_version_fallback.py` (new, `cluster`/
  `slow`-marked like its sibling `test_cli_fallback.py`): a real
  primary+replica pair, a real failover, and a real `version restore`
  attempt while parked on the fallback — rejected, while `version list`
  (a read) keeps working. This was the one acceptance-criterion item not
  yet covered by `TASK-00182`'s own verification.
- Full existing suite re-run for regressions: `ctest` (18/18,
  `build-msvc-105`), full `tests/integration -m "not cluster"` (97
  passed), plus the new cluster-marked test (1 passed). Zero failures,
  zero skips beyond the pre-existing binary-not-found skip guards.

What this task does NOT cover, and why, recorded rather than silently
dropped: no automated test exists for the GUI dialog itself
(`TASK-00183`'s "Version History..." panel) — this project has no
ImGui/GUI test harness anywhere (checked; genuinely nothing to extend),
and this session had no display to drive one manually. `TASK-00183`'s own
notes already flag its verification as compile/link/static-review only,
not interactive. Building a real GUI test harness would be new
infrastructure, not a regression test for this feature — out of scope
here; worth its own task if automated GUI coverage is wanted generally,
not just for this one dialog.

Sign-off: acceptance criteria met for the daemon IPC and CLI surface,
which is everything this task's own Work section actually asked for.
Moving to `done`.
