---
id:          TASK-00239
title:       "Add the Android app to the release cycle (release.yml)"
status:      done
assignee:    BLD.05
created_by:  ARCH.00
created:     2026-09-02
priority:    normal
depends_on:  [TASK-00233]
blocks:      [TASK-00236]
review_by:   [CQR.08]
tags:        []
---

`.github/workflows/release.yml` (see `docs/RELEASE.md`) currently has exactly
three jobs — `build-linux`, `build-windows`, `publish` — and produces zero
Android artifacts. `TASK-00233` adds Android to `ci.yml` (build **validation**
only, `./gradlew assembleDebug` on every push) but was explicitly scoped to
CI, not the release pipeline; nothing wires an APK into an actual tagged
GitHub Release. Filed while auditing the Android milestone
(`ARCHITECTURE.md`'s Phase 21) for what's still missing before it can be
marked complete — this is a genuine gap, not a maybe.

Scope:
- New `build-android` job in `release.yml`: set up the same JDK/SDK/NDK
  toolchain `TASK-00233`'s CI job establishes, run a release build
  (`./gradlew assembleRelease` or `bundleRelease` — decide which; an AAB is
  what Play Store submission actually wants, an APK is what direct/sideload
  distribution wants, and this project doesn't have a Play Store presence
  yet, so the right answer here isn't obvious and depends on how the app is
  meant to reach users first), and attach the resulting artifact (+
  `.sha256`, matching the existing Linux/Windows artifacts' convention) to
  the `publish` job's GitHub Release.
- **Signing is an open question, not a detail to quietly resolve alone**: an
  Android release build must be signed to be installable outside of
  `adb install`. This project has no signing keystore or Play Store
  presence today. Do not silently ship a debug-signed "release" APK and
  call it done — either set up a real release keystore (stored as a GitHub
  Actions secret, matching how this workflow already treats `SDL2_ZIP_SHA256`/
  `WIX_ZIP_SHA256`-style supply-chain-sensitive material) with ARCH.00/the
  user's sign-off, or explicitly document (in `docs/RELEASE.md`, alongside
  the existing per-platform artifact table) that the attached build is
  debug-signed / sideload-only until that decision is made, so nobody
  mistakes it for something it isn't.
- Update `docs/RELEASE.md`'s "What the workflow does" table to add the new
  job/artifact row, matching its existing per-platform documentation style.

## Acceptance criteria

- A tagged release produces an Android build artifact attached to the
  GitHub Release, the same way Linux/Windows artifacts already are.
- The signing question above has an explicit, documented answer (either a
  real signing setup, or a clearly-labeled sideload-only interim artifact)
  — not silently defaulted to whatever `assembleRelease` does out of the
  box.
- `docs/RELEASE.md` accurately describes the new job.

## Notes

ARCH.00, 2026-09-02: Filed in response to the user asking whether Android
was already in the release cycle (it wasn't) — added as a blocker on
`TASK-00236` (Android milestone closure) so that task cannot mark the
milestone complete while this gap still exists.

BLD.05, 2026-09-03: Both open questions resolved with the user's sign-off
before implementing:

- **Artifact type**: APK via `assembleRelease` (not AAB) — this project has
  no Play Store presence, and an AAB isn't directly sideloadable.
- **Signing**: debug-signed, documented as sideload-only — no real
  signing keystore exists yet, and none is being set up as part of this
  task. `android/app/build.gradle`'s `release` build type now explicitly
  sets `signingConfig signingConfigs.debug` (previously unset, which would
  have produced an unsigned, uninstallable release APK).

Implementation:
- New `build-android` job in `.github/workflows/release.yml`, reusing the
  same SDK/NDK/CMake component set `ci.yml`'s `build-android` job
  (`TASK-00233`) installs. Runs `./gradlew assembleRelease`, stages the
  APK as `vaporwault-<tag>-android.apk` with a `.sha256` sidecar matching
  the Linux/Windows artifact convention, and uploads it.
- `publish` job now depends on `build-android` too and attaches
  `dist/android-release/*` in both the create and upload (`--clobber`)
  branches.
- `docs/RELEASE.md`: added the new job to the "What the workflow does"
  table, a new "Android APK" subsection documenting the debug-signing
  decision and what it does/doesn't guarantee, and updated the dry-run
  (`workflow_dispatch`) description to mention all three build jobs.

Not done here (explicitly out of scope, called out above and in
`docs/RELEASE.md`'s new section): setting up a real release keystore.
That's a follow-up task to file against BLD.05 if/when this project gets
an actual distribution channel that needs one.

## Review

CQR.08, 2026-09-04: Reviewed `release.yml`'s new `build-android` job,
`android/app/build.gradle`'s `signingConfig` addition, and the
`docs/RELEASE.md` updates. No blocking findings.

- Signing/artifact decisions match what's documented in this task's own
  notes and in `docs/RELEASE.md`'s new "Android APK" section — the
  sideload-only framing is explicit and can't be mistaken for a
  production release artifact.
- `signingConfig signingConfigs.debug` on the `release` build type is
  correct and necessary — without it, `assembleRelease` would produce an
  unsigned, uninstallable APK (verified in this session: this was
  previously unset, and the release build type had no signing config at
  all).
- **Advisory, not blocking** (also flagged while reviewing `TASK-00242`,
  which touches the same pattern): the SDK/NDK/CMake component
  install step this job uses is now duplicated three times across
  `ci.yml` (`build-android`, `test-android-instrumented`) and
  `release.yml` (`build-android`). Each job's own comments already
  acknowledge this ("Same rationale as build-android above") rather than
  silently copy-pasting, and it matches this project's existing
  no-composite-actions convention — but a future NDK/CMake version bump
  needs to touch all three in lockstep. Worth a shared composite action
  or reusable workflow if a fourth job ever needs the same block; not
  worth the churn for three.

**Status: done.** No blocking findings; the one advisory note above is a
minor, pre-existing-pattern-consistent duplication concern, not a defect.

Moved to `TODO/review/` for CQR.08 sign-off.
