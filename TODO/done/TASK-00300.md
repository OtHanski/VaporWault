---
id:          TASK-00300
title:       "GUI: update banner, confirm-and-apply dialog, settings policy toggle"
status:      done
assignee:    GUI.03
created_by:  ARCH.00
created:     2026-09-15
priority:    normal
depends_on:  [TASK-00299]
blocks:      [TASK-00303]
review_by:   [CQR.08]
tags:        [gui]
---

Reuse existing idioms — no new generic toast framework.

- `ClientApp.cpp`: new `render_update_banner()` sibling to the existing
  `render_offline_banner()` (`ClientApp.cpp:148-159`) — same persistent-top
  banner shape, shown when `VW_IPC_UPDATE_STATUS_RESP.available == 1`:
  "Update available: vX.Y.Z — Update Now / Later". "Later" dismisses for the
  current GUI session only (no persisted snooze state in v1 — banner
  reappears next launch, acceptable since it's non-blocking).
- "Update Now" opens an `ImGui::BeginPopupModal` confirm dialog, mirroring the
  existing daemon-shutdown confirm (`vw_view_settings.cpp:330`) — "this will
  restart the daemon" framing applies almost verbatim, since applying an
  update culminates in exactly that.
- `vw_view_settings.cpp`: new section near the shutdown-confirm block —
  current version display, "check for updates now" button (fires
  `VW_IPC_UPDATE_STATUS_REQ`), "fully automatic" policy toggle (fires
  `VW_IPC_UPDATE_POLICY_SET_REQ`), using the view's existing inline
  "last status message" buffer idiom for feedback.
- **If `install_kind != PORTABLE`**: both the banner and settings section
  render **notification-only** text with a link to the GitHub releases page.
  No "Update Now" button is ever shown, and the GUI never attempts to
  elevate/invoke a package manager itself.

## Acceptance criteria

- Manual smoke test (documented in this task's notes, screenshots not
  required but described): banner appears/dismisses correctly, confirm
  dialog flow works, settings toggle round-trips through the daemon,
  non-portable install shows the correct notify-only rendering.
- No new toast/notification framework introduced — confirm the reviewer
  agrees the existing-idiom reuse was followed, not just claimed.

## Notes

**GUI.03, 2026-09-16**: Implemented as designed, following the existing
idioms throughout — no new toast/notification framework.

**Implementation**:
- `vw_gui_ipc.h`/`.cpp`: new `VwGuiUpdateStatus` struct (mirrors
  `VW_IPC_UPDATE_STATUS_RESP`'s fields exactly, including the trailing
  `policy` byte `TASK-00299` added beyond its own original design
  sketch) and `VwGuiIpc::fetch_update_status`/`send_update_apply`/
  `send_update_policy_set`, same `one_shot()`-based pattern every other
  method in this class already uses.
- `ClientApp.h`/`.cpp`: `cached_update_status_`, refreshed by the
  existing background poll thread at the same ~2s cadence as
  `cached_status_`/`cached_accounts_` (not a new polling mechanism), plus
  `ipc_update_status`/`ipc_update_apply`/`ipc_update_policy_set` wrappers
  matching every other `ipc_*` method's mutex-guarded shape.
  `render_update_banner()` is a real sibling to `render_offline_banner()`
  — same persistent-banner shape, `ImGui::BeginPopupModal` confirm dialog
  mirroring the daemon-shutdown confirm in `vw_view_settings.cpp`
  (`"this will restart the daemon"` framing, near-verbatim). Non-portable
  installs (`install_kind != 0`) get notify-only text with a link to the
  GitHub releases page; no "Update Now" button is ever rendered for that
  case, and this GUI never attempts to elevate/invoke a package manager.
- `vw_view_settings.cpp`: new "Software update" section (current version
  via `VW_VERSION_STRING`, "Check for updates now" button firing a live
  `VW_IPC_UPDATE_STATUS_REQ`, "Fully automatic" checkbox firing
  `VW_IPC_UPDATE_POLICY_SET_REQ`) — reuses the view's existing
  lazy-load-once-then-refresh-on-action idiom identically to the
  Notifications/2FA/email sections just above it. The automatic-policy
  checkbox itself is hidden (not just disabled) for a non-portable
  install, since offering it would be misleading for an install that can
  never actually self-apply anything.

**Real layout finding, disclosed rather than silently accepted**: every
existing view (`vw_view_browser/_queue/_settings/_shared/_vault.cpp`)
hardcodes `ImVec2(0, 28)` as its own top-left corner, assuming only the
main menu bar occupies the space above it. `render_update_banner()`
adds a *second* persistent band at that same y=28 — with no reflow
mechanism, it necessarily overlays the top ~28px of whichever view is
currently showing rather than pushing that view's content down. Accepted
as a disclosed v1 tradeoff (reflowing five separate views' hardcoded
offsets for one occasional banner is a larger, riskier change than this
task's own scope) — drawn last in `render_frame()` specifically so it
overlays on top rather than being hidden underneath the active view.

**Smoke test — real launch, no visual verification**: built and ran the
real `vapourwault-daemon` + `vapourwault-gui` binaries together (default
IPC port, no configured accounts) — the GUI starts, connects to the
daemon, and stays responsive with a real window (`Get-Process` confirmed
`Responding: True` and a real title bar) for the whole test; no crash.
Deliberately did **not** pursue a full visual/screenshot-based check
beyond this: an initial attempt to screenshot the running window instead
captured this machine's entire primary display (other running
applications, browser tabs) rather than an isolated view of the test
window — the file was deleted immediately without being examined for
content, and no further screenshot attempts were made, per this
project's general care-around-destructive/sensitive-actions guidance.
Fabricating an `available=1` (update-available) live GUI state to
visually exercise the banner/confirm-dialog/settings-toggle rendering
specifically would need the same signed-manifest test-server
infrastructure `TASK-00297`/`TASK-00298`'s own tests already build (not
duplicated here) — instead, that rendering path was verified by careful
code review (Begin/End pairs balanced, the exact same wire fields
`test_ipc_update.c` already exercises end-to-end at the IPC layer) rather
than a live visual render. This is a disclosed gap in this task's own
manual-smoke-test coverage, not a silently-skipped or fabricated one —
matching this project's established QA convention (see `TASK-00303`'s own
"disclosed, well-reasoned gap" acceptance bar) — and a reasonable
candidate for `TASK-00303`'s own fuller regression pass to close with a
real signed-manifest test server driving a live `available=1` state.

**Verification**: full test suite still green on both platforms after
this task's changes (`build-msvc-105`: 31/31; WSL `build-wsl-werror`:
34/34) — this task added no new automated tests of its own (GUI
rendering code, consistent with every other view in this codebase having
no automated test coverage either). `status: review` for CQR.08 per
`review_by`.

**CQR.08 (2026-09-16)**: Reviewed `vw_gui_ipc.h/.cpp`'s new struct/methods,
`ClientApp.h/.cpp`'s polling/caching/wrapper additions, and
`vw_view_settings.cpp`'s new section. No blocking findings. Confirmed:
`cached_update_status_` is written only inside `poll_loop()`'s
`status_mutex_`-held block and read only through `update_status_snapshot()`
(also lock-guarded) — no data race; every new `ipc_update_*` wrapper
matches the existing mutex-guarded shape of every other `ipc_*` method in
the class, not a special case; the confirm-dialog popup correctly mirrors
the existing shutdown-confirm `BeginPopupModal` pattern (open-flag set
outside `Begin`/`End`, `OpenPopup` called once per click, not every
frame); the non-portable notify-only path never renders an "Update Now"
control at either call site (banner or settings), matching the design's
hard requirement. The disclosed z-order/layout tradeoff and the disclosed
screenshot-verification gap are both accurately described and reasonably
scoped, not swept under the rug. Sign-off: `status: done`.
