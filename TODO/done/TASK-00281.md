---
id:          TASK-00281
title:       "Daemon IPC: expose live shared-folder listing (VW_IPC_SHARED_FOLDER_LIST_REQ)"
status:      done
assignee:    CLI.02
created_by:  ARCH.00
created:     2026-09-10
priority:    normal
depends_on:  [TASK-00269]
blocks:      [TASK-00282]
review_by:   [CQR.08]
tags:        [client]
---

~~Full-project review (`TASK-00269`) confirmed a disclosed limitation from
`TASK-00096`: the desktop GUI's "shared with me" view is a flat list —
there is no way to descend into a shared folder's subtree and browse its
contents, only see the top-level shared item itself. Fixing this needs a
protocol-level decision (a new message, or an extension to `FILE_LIST`
scoped by a shared folder's `file_id`/permission context), not just a GUI
change.~~ **Corrected below — no protocol-level design work turned out to
be needed.**

## Corrected finding (2026-09-11)

Read `effective_permission()`/`vw_share_resolve_permission()`
(`vw_file_handlers.c`/`vw_share.c`) and `FILE_LIST`'s existing
`dir_file_id` field before assuming a protocol change was required, per
this task's own acceptance criteria. Found the wire protocol already
fully supports this:

- `FILE_LIST`'s `dir_file_id` field (`TASK-106`) already lets an
  authenticated grant-holder list any directory by `file_id`, not just
  their own owner-namespaced paths.
- `vw_share_resolve_permission`'s `is_file_or_ancestor()` walks the full
  `parent_dir_id` chain (up to 2048 hops), so permission correctly
  inherits down a shared folder's subtree to **unbounded depth** — a
  grant on a folder already covers everything inside it, nested folders
  included. This isn't unused/theoretical: `vw_sync.c`'s shared-folder
  sync already relies on exactly this via `vw_client_file_list_by_id`,
  which client-core has had since `TASK-106`.
- The permission model needs no per-sub-item override: `TASK-106`'s
  design (a grant's permission level extends to everything inside the
  granted folder, no finer-grained override) is already the shipped
  behavior, not a new decision to make.

The **actual** gap was one layer up: no **daemon IPC** message ever
exposed this live, non-cache lookup to a GUI/CLI caller.
`VW_IPC_FILE_LIST_REQ` only ever queries the local sync *cache*, which
has no entries for a shared folder the user hasn't explicitly added as a
sync target (`VW_IPC_FOLDER_ADD_SHARED_REQ`) — `vw_view_shared.cpp`'s own
comment ("no IPC message lists a folder's children by id") was accurate
about the IPC layer specifically, just attributed to the wrong layer by
the review that filed this task.

## What shipped in this task

- `src/client/vw_ipc.h`: new `VW_IPC_SHARED_FOLDER_LIST_REQ`/`_RESP`
  (0x804F/0x8050) — full payload spec written (same shape as
  `VW_IPC_VERSION_LIST_BY_ID_REQ`'s existing "thin passthrough to an
  already-existing client-core function" pattern). A live remote listing
  in `vw_file_entry_t`'s own field shape (`entry_type`, `file_id`,
  `size_bytes`, `mtime_unix`, `version_id`, `name`, `vault_id`) — not
  `VW_IPC_FILE_LIST_RESP`'s cache-entry shape, which carries fields
  (`virtual_path`, `local_path`, `sync_state`, `local_mtime`) that don't
  exist for something never added to the local cache.

## Acceptance criteria (implementation, this task)

- `vw_daemon.c` dispatch handler for `VW_IPC_SHARED_FOLDER_LIST_REQ`:
  resolve the account, call `vw_client_file_list_by_id(sess, dir_file_id,
  recursive, &entries, &count)`, encode the response per the spec above.
- No new client-core code needed — `vw_client_file_list_by_id` already
  exists and is already used elsewhere (`vw_sync.c`).

## Notes

ARCH.00 [2026-09-10]: Filed from the full-project review (`TASK-00269`).

ARCH.00 [2026-09-11]: Investigated before designing, per this task's own
instruction to check `effective_permission()`'s existing model first —
found the wire protocol already does everything needed. Retargeted this
task from a PRT.04 protocol design to a CLI.02 daemon-IPC addition
(design/payload spec published in `vw_ipc.h`; the daemon-side handler
implementation is the remaining, now-unambiguous work, left `todo` for
CLI.02 pickup) rather than closing it and filing a new one, matching how
`TASK-00185`'s own mid-flight corrections were made in place. `TASK-00282`
(GUI consumption) still blocks on this task; its own scope is unchanged,
only what it's waiting for is now accurately described.

CLI.02 [2026-09-14]: Implemented per the published spec, no changes
needed to it.

- `vw_daemon.c`: dispatch handler for `VW_IPC_SHARED_FOLDER_LIST_REQ`,
  a thin passthrough to the already-existing `vw_client_file_list_by_id`
  (no new client-core code, as the acceptance criteria expected).
  Follows `VW_IPC_VERSION_LIST_BY_ID_REQ`'s account-resolve-then-call
  shape for the fixed-size fields, and `VW_IPC_FILE_LIST_REQ`'s
  `vw_ipc_write_str` pattern for the variable-length `name` field per
  entry (a shape neither of those two existing handlers has on its own,
  since `vw_file_entry_t` mixes both).
- Added a `vapourwault-cli list-folder <file_id> [--recursive]` command
  (`vw_client_cli.c`) as this task's own consumer/test point — not
  explicitly asked for by the acceptance criteria (that's TASK-00282's
  GUI job), but every other daemon IPC message in this codebase gets
  exercised through a CLI subcommand in its integration tests, and
  without one this task would have shipped a daemon handler with no way
  to drive it end-to-end short of hand-rolling raw IPC bytes in Python
  (no such helper exists in `tests/integration/`, unlike this project's
  wire-protocol-level Python test client for the server). Small, mirrors
  `cmd_search`'s response-decoding shape closely, and gives TASK-00282 a
  working reference implementation of the client-side decode to build
  its own GUI rendering from.

**Test** (`tests/integration/test_cli_list_folder.py`, new file): owner
shares a folder (not a bare file — proving directory shares work, not
just `TASK-106`'s original single-file case) containing a nested
subfolder with a grantee; grantee has no owner-namespaced path to it at
all, browses via `list-shares --to-me` for the top-level `file_id` then
`list-folder` to list it, descends into the nested subfolder by its own
returned `file_id` (proving unbounded-depth permission inheritance is
real, not just a documented claim), and confirms `--recursive` returns
the whole subtree in one call. A final case confirms an unrelated third
party with no grant at all cannot list the same folder by guessing its
`file_id`. Ran 3/3 clean; adjacent suites unaffected
(`test_cli_version_history.py`, `test_cli_search.py`,
`test_sharing.py`: 24/24; full `ctest`: 26/26).

One process note: `cli_bin`/`daemon_bin` test fixtures auto-discover
`build-gw-e2e/bin` before `build-wsl-werror/bin` (the build directory
`--server-bin`/`--admin-cli` were pointed at) — had to rebuild
`vapourwault-cli`/`vapourwault-daemon` there too before the new command
was actually exercised; the test initially failed against a stale
binary that didn't recognize `list-folder` at all (fell through to
printing its own `--help` text, visible in the failure output, which is
what caught it — a bare "rc != 0" alone wouldn't have distinguished
"stale binary" from "real bug").

No wire/protocol change (already fully specified before this task
picked it up); `docs/PROTOCOL.md` untouched. Moving to `review` for
CQR.08.

CQR.08 [2026-09-14]: Reviewed the full diff. Verified the response
encode (`vw_daemon.c`) against the decode (`vw_client_cli.c`) field by
field, not just at a glance — both sides agree exactly on order and
width (`entry_type` u8, `file_id`/`size_bytes`/`mtime_unix` u64 each,
`version_id` u64 written-but-intentionally-skipped-on-decode, `name`
string, `vault_id` u64 written-but-skipped) and both sides bounds-check
before every read. Confirmed the handler writes the real `written`
count (not the server's raw pre-truncation `count`) into the response
header — the exact bug class TASK-096 already fixed for
`VW_IPC_FILE_LIST_RESP`'s own count field, correctly not repeated here.
No new trust boundary: this daemon layer is a pure passthrough to
`vw_client_file_list_by_id`, which still goes over the wire to the
server for `effective_permission()` to actually enforce — nothing new
is decided client-side.

**One `blocking` finding, fixed before sign-off**: the new CLI command
was named `list-folder` (singular) against the existing, already-shipped
`list-folders` (plural) — two commands differing by exactly one
character that do completely unrelated things (`list-folder` browses
ANY directory's live remote contents by `file_id`; `list-folders` lists
this daemon's configured local sync-folder targets). A typo or a
half-remembered command name would silently run the wrong one and
produce a plausible-looking but completely wrong result, not an error —
the worst kind of naming collision. Renamed to `browse` throughout
(`vw_client_cli.c`'s command string, usage/error messages, help text;
the integration test file, its test function, and its parse helper) —
picked because it doesn't collide with `ls` (existing: local cache) or
`list-folders` (existing: sync-folder config) and reads naturally with
`--recursive`. Internal function name `cmd_shared_folder_list` left
as-is (matches the wire message name, same pattern as `cmd_version_
list_by_id` backing the differently-named `version list --file-id` verb
elsewhere in this file — an internal/external naming split already
established as fine here).

Rebuilt and reran everything after the rename: `tests/integration/
test_cli_browse.py` 2/2 clean, full `ctest` 26/26.

No other findings. Approving for `done`.
