---
id:          TASK-00221
title:       "GUI: Settings view caches folder/notification data across account switches"
status:      done
assignee:    GUI.03
created_by:  GUI.03
created:     2026-08-27
priority:    low
depends_on:  []
blocks:      []
review_by:   [CQR.08]
tags:        [gui]
---

Discovered while implementing `TASK-00210` (notification-preferences panel).
`vw_view_settings.cpp`'s static, single-window-always-open state
(`s_folders`/`s_folders_loaded`, and now `s_notify_prefs`/
`s_notify_loaded`) is never invalidated when the active account changes.
`ClientApp::switch_active_account()` already invalidates the browser/
shared/vault views (`vw_view_browser_invalidate()` etc.) but has no
equivalent call for the settings view — confirmed by reading
`switch_active_account()` and grepping this file for any account-id
comparison or invalidation call; there is none.

Practical effect: switching accounts while the Settings tab is open (or
switching, then opening Settings) shows the *previous* account's sync
folders and notification preferences until some other action happens to
call `refresh_folders`/`refresh_notify_prefs` again (e.g. clicking
"Refresh"). Not a security issue (never sends a request against the
wrong account_id — `ClientApp::ipc_notify_prefs_get/set` and
`ipc_folder_*` all read `active_account_id_` fresh at call time; this is
purely a stale-display bug), but a real, user-visible correctness gap.

Pre-existing for `s_folders`/`s_folders_loaded` already, before this
task — `TASK-00210` inherited the same pattern for consistency with the
surrounding code rather than fixing only the new half and leaving the
older half inconsistently behaved, and is filing this rather than
silently expanding its own scope to fix both.

## Work

- Add a `vw_view_settings_invalidate()` (matching the naming convention
  `vw_view_browser_invalidate()` etc. already use) that resets
  `s_folders_loaded`/`s_notify_loaded` to false, called from
  `ClientApp::switch_active_account()` alongside the other three.

## Acceptance criteria

- Switching the active account while the Settings tab is open (or before
  opening it) shows that account's own folders and notification
  preferences on next render, not the previously-active account's.

## Notes

<!-- Agents append notes below with their ID and date. Do not delete prior notes. -->

GUI.03 [2026-08-27]: Filed while implementing `TASK-00210` — found by
reading `switch_active_account()` and this file's own state management
in full before assuming either was correct.

GUI.03 [2026-08-27]: Implemented. Added `vw_view_settings_invalidate()`
(`vw_view_settings.h`/`.cpp`), resetting `s_folders_loaded`/
`s_notify_loaded` per this task's own scope, plus `s_email_loaded`
(`s_folders`/`s_selected_folder`/`s_folder_rules_status` also reset for
the same folders-view staleness this task already flagged as
pre-existing) — `s_email_loaded` didn't exist when this task was filed;
it was added by `TASK-00222`'s "Account email" section afterward and has
the identical staleness bug, so it's covered by the same fix rather than
left as a second unfixed instance of exactly what this task describes.

Wired into `ClientApp::switch_active_account()` alongside the other
three `_invalidate()` calls. Also found and fixed the same bug at a
second call site this task's own investigation didn't mention:
`ClientApp::ipc_account_remove()` has its own separate
browser/shared/vault-invalidate block (for when the removed account was
the active one) that was missing the settings call too — same root
cause, same fix, fixed in the same pass rather than left as a
now-obviously-identical gap.

Verified on both toolchains: WSL/GCC (`build-gw-e2e`) and MSVC
(`build-msvc-105`) build the GUI target clean. Not independently
integration-tested (no existing test harness drives the ImGui GUI's
rendered state — same limitation this project's GUI code generally has)
— verified by code inspection (`grep -n "active_account_id_\s*="`) that
there are exactly three places `active_account_id_` is ever assigned:
`switch_active_account`/`ipc_account_remove` (both now call
`vw_view_settings_invalidate()`) and the poll loop's auto-select-the-
sole-configured-account branch, which only fires while
`active_account_id_` is still the zero sentinel — i.e. there is never a
previously-real active account whose settings data could be stale at
that third site, so it needs no invalidate call of its own.

CQR.08 [2026-08-27]: Reviewed. `vw_view_settings_invalidate()` correctly
mirrors the reset shape `vw_view_browser_invalidate()` already
establishes (reset the `_loaded` flags plus any dependent display
state, not just the flags) — `s_folders.clear()`/`s_selected_folder=-1`/
clearing `s_folder_rules_status` are the right accompanying resets so
the folder-detail sub-panel doesn't keep pointing at a stale index into
now-cleared data. Confirmed the fix covers `s_email_loaded` even though
the task predates it, and confirmed (independently re-ran the same
`grep`) the three-call-site claim about `active_account_id_`
assignments and the reasoning for why the third needs no fix. No
`blocking` findings. `status: done` approved.
