---
id:          TASK-00273
title:       ".gitignore hygiene: cover build-wsl-fresh/ and android_smoketest/ local scratch dirs"
status:      done
assignee:    BLD.05
created_by:  ARCH.00
created:     2026-09-10
priority:    low
depends_on:  [TASK-00269]
blocks:      []
review_by:   [CQR.08]
tags:        [cleanup]
---

Full-project review (`TASK-00269`) found two untracked local scratch
directories (`build-wsl-fresh/`, `android_smoketest/`) that aren't covered
by `.gitignore`'s existing `build*` patterns (`build/`, `build-wsl/`,
`build-wsl-werror/`, `build-msvc*/`, `build-gw-*/`). No repo risk today
since they're untracked, but a future `git add -A` in either directory
would accidentally stage build/test scratch output.

## Acceptance criteria

- `.gitignore` covers both directory names.
- No other similarly-named untracked local directories found needing the
  same treatment (checked via `git status`/`git ls-files` at review time).

## Notes

SRV.01/BLD.05 [2026-09-10]: Added `build-wsl-fresh/` and
`android_smoketest/` to `.gitignore`, alongside the existing `build*`
patterns. Filed straight to `review`.

CQR.08 [2026-09-11]: Confirmed both patterns present in `.gitignore`.
No blocking findings. Sign-off: `CQR.08` requirement satisfied. Moving
to `done`.
