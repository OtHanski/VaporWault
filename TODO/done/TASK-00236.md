---
id:          TASK-00236
title:       "Close the Android client milestone"
status:      done
assignee:    ARCH.00
created_by:  ARCH.00
created:     2026-08-31
priority:    normal
depends_on:  [TASK-00235, TASK-00239]
blocks:      []
review_by:   [CQR.08]
tags:        []
---

Mark `TASK-00224`–`235` done once QA.06 signs off, and update
`ARCHITECTURE.md`'s Phase 21 row ("Android client") from "in progress" to
"complete" — plus record here any decisions that emerged during
implementation that the design stage (`TASK-00224`) didn't anticipate, the same
way every prior phase's closure has done.

## Acceptance criteria

- Phase 21's row in `ARCHITECTURE.md`'s Implementation Phases table reflects
  reality (checked against `TODO/`'s actual task statuses, not memory, per
  this document's own stated audit discipline).
- Any mid-implementation design deviations are recorded in the Architectural
  Decisions table, not left undocumented.

## Notes

ARCH.00, 2026-09-02: Added `TASK-00239` (Android build in the release cycle,
`release.yml`) as a dependency — the user asked whether the Android app was
already part of the release cycle, it wasn't, and this milestone should not
be closeable while that gap exists.

ARCH.00, 2026-09-04: Verified readiness before doing any closure work —
walked every task file `TASK-00224`–`00246` and confirmed all are
`status: done` in `TODO/done/`; listed `TODO/todo/` and `TODO/review/` and
confirmed the only other open task anywhere in the project is `TASK-00152`
(an unrelated Windows MSI verification task), which this milestone does not
depend on. Both this task's own `depends_on` entries (`TASK-00235`,
`TASK-00239`) are done, and so are every follow-on task filed along the way
(`TASK-00240`–`00246`).

Closed the milestone:

- `ARCHITECTURE.md`'s Phase 21 row changed from "in progress" to
  **complete**, with a closing narrative appended covering `TASK-00239`
  through `TASK-00246` (release-cycle fix, the two RecyclerView-in-ScrollView
  bugs found and fixed beyond `TASK-00241`'s original one, the accept-loop
  DoS, the SAF-staging reversal, and the CQR.08 review findings fixed in each
  round) — full text in that row, not duplicated here.
- Two real mid-implementation design deviations found and recorded in the
  Architectural Decisions table, per this task's own acceptance criteria:
  - **Android release artifact & signing** (new row): `TASK-00224`'s design
    never addressed distribution at all; `TASK-00239` shipped a debug-signed,
    sideload-only APK rather than standing up Play Store signing/listing
    infrastructure for one target.
  - **Android on-demand transfer storage** (amended existing row): the
    original design said transfers stage through the app's private cache;
    `TASK-00228` deliberately dropped that in favor of direct streaming, and
    `TASK-00246` then reintroduced staging — but only for the upload picker
    path, only as a synchronous pre-dispatch copy, and for a different
    reason (an SAF grant not surviving to the async background job) than the
    original design's rationale. The row now reconciles both reversals
    instead of describing either as final.
- Updated the top-of-file "Last updated" changelog entry following this
  document's own established convention (prepend, cite the task, summarize
  what changed), superseding the two oldest entries rather than keeping them
  verbatim, matching what the 2026-08-05 entry did previously.

No further design deviations worth recording turned up beyond the two above
— the CI job additions (`TASK-00239`'s and `TASK-00242`'s), the RecyclerView
fixes, the vault-ID-lookup test fix, and the accept-loop DoS fix are all
either already covered by an existing Architectural Decisions row or are
implementation detail rather than a design-level choice, so they're recorded
only in Phase 21's own narrative, not as new table rows.

Moving to `TODO/review/` for CQR.08 sign-off per `review_by`.

## Review

CQR.08, 2026-09-04: Checked both acceptance criteria against the actual
repo state, not against this task's own notes.

- Re-walked `TODO/todo/`, `TODO/review/`, `TODO/done/` independently:
  confirmed `TASK-00224`–`00246` are all `status: done` in `TODO/done/`, and
  the only other open task anywhere is the unrelated `TASK-00152`. Phase
  21's row in `ARCHITECTURE.md` now reads **complete** and its closing
  narrative's task-ID citations (`TASK-00239`–`00246`) match what those task
  files themselves say happened — cross-checked each one's title/assignee/
  fix summary against the narrative rather than trusting it on sight.
  Acceptance criterion 1 met.
- Verified the two new/amended Architectural Decisions rows: the "Android
  on-demand transfer storage" row's before/after actually matches
  `TASK-00228`'s and `TASK-00246`'s own task files (confirmed the "no
  private-cache staging" and "SAF grant doesn't survive to the background
  job" claims against those two files directly, not just against this
  task's notes) and correctly scopes the reintroduced staging to the
  upload-picker path only — checked `FileBrowserActivity.kt`'s `uploadFile`
  and `VaultTransferer.kt`'s `file://`-scheme short-circuit to confirm
  downloads and non-picker vault calls weren't also changed. The new
  "Android release artifact & signing" row's claim about
  `android/app/build.gradle` previously having no `signingConfig` at all
  checked out against `TASK-00239`'s own diff. Acceptance criterion 2 met.
- Markdown integrity: both edited table rows still have exactly 4 `|`
  separators (3-column table preserved) and the Phase 21 row still has 6 (5-
  column table preserved); no stray pipe characters introduced by the new
  prose. Bold-marker (`**`) count across the whole file is even — no
  unclosed emphasis spans.
- Checked no other document makes a stale "in progress" claim about the
  Android milestone that this closure missed: `README.md` only links
  `docs/ANDROID_BUILD.md` (toolchain instructions, not a status claim);
  `docs/PROTOCOL.md`'s one "Phase 21" mention is historical framing, not a
  status claim. Nothing else needed updating.
- No blocking findings. `status: done`.
